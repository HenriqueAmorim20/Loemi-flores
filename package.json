{
  "name": "Loemi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt NODE_OPTIONS=--openssl-legacy-provider",
    "build": "nuxt build NODE_OPTIONS=--openssl-legacy-provider",
    "start": "nuxt start NODE_OPTIONS=--openssl-legacy-provider",
    "generate": "nuxt generate NODE_OPTIONS=--openssl-legacy-provider"
  },
  "dependencies": {
    "core-js": "^3.19.3",
    "nuxt": "^2.15.8",
    "vue": "^2.6.14",
    "vue-server-renderer": "^2.6.14",
    "vue-template-compiler": "^2.6.14",
    "vuetify": "^2.6.1",
    "webpack": "^4.46.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.12.3"
  }
}
